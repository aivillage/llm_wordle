# llm_wordle
Challenges for LLM

# Building

It should be as easy as `npm install` then `npm build`. Then run `uvicorn app.main:app --reload`. also do `npx tailwindcss -i ./assets/css/styles.css -o ./app/static/css/styles.css --watch`